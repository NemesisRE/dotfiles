#!/usr/bin/env zsh
#
# vim: ts=2 sw=2 et ff=unix ft=zsh syntax=zsh
# chezmoi-generated monolithic bundle of all zsh nredf shell functions.
# Generated on chezmoi apply — loaded in <1ms on shell startup.
#
# File list derived from the functions/ directory — see common/functions.bundle.tmpl.
{{ $dir := "dot_local/share/nredf/shell/zsh/functions" -}}
{{ range (glob (joinPath .chezmoi.sourceDir $dir "*")) -}}
{{- $rel := printf "%s/%s" $dir (base .) }}
{{ if hasSuffix ".tmpl" $rel }}{{ includeTemplate $rel $ }}{{ else }}{{ include $rel }}{{ end -}}
{{ end -}}
