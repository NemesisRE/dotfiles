# Editor-side shellcheck defaults, deployed to ~/.config/shellcheckrc.
# The repo-root .shellcheckrc (what CI uses) disables the same SC1090/SC1091; keep them in sync.
external-sources=true
disable=SC1001
disable=SC1090
disable=SC1091
