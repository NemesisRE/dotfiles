# nredf: your machine-local PowerShell aliases.
#
# Dot-sourced on shell startup. chezmoi seeded this file once, empty, and
# will never overwrite it again (the `create_` attribute) — edit it freely.
#
# Example:
#   Set-Alias -Name ll -Value Get-ChildItem
#
# For a simple alias shared across every shell, ~/.config/nredf/local.yaml
# is usually less to write — see ~/.config/nredf/README.md.
