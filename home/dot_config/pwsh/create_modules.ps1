# nredf: your machine-local PowerShell module imports.
#
# Dot-sourced on shell startup, right after NREDF's own Modules.ps1
# (which requires none). chezmoi seeded this file once, empty, and will
# never overwrite it again (the `create_` attribute) — edit it freely.
#
# Example:
#   Import-Module posh-git
