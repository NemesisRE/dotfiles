# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
# Alias for lazyjournal (home/.chezmoidata/aliases.yaml): hands the whole
# command line to lazyjournal's completer.
name: lj
description: Alias for lazyjournal
parsing: disabled
completion:
  positionalany: ["$carapace.bridge.CarapaceBin([lazyjournal])"]
