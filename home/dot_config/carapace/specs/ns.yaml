# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
# Alias for kubens (home/.chezmoidata/aliases.yaml): hands the whole
# command line to kubens's completer.
name: ns
description: Alias for kubens
parsing: disabled
completion:
  positionalany: ["$carapace.bridge.CarapaceBin([kubens])"]
