# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
# Alias for kubectl (home/.chezmoidata/aliases.yaml): hands the whole
# command line to kubectl's completer.
name: k
description: Alias for kubectl
parsing: disabled
completion:
  positionalany: ["$carapace.bridge.CarapaceBin([kubectl])"]
