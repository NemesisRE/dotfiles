# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
# Alias for kubectx (home/.chezmoidata/aliases.yaml): hands the whole
# command line to kubectx's completer.
name: kctx
description: Alias for kubectx
parsing: disabled
completion:
  positionalany: ["$carapace.bridge.CarapaceBin([kubectx])"]
