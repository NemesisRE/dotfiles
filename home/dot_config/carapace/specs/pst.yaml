# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
# Alias for procs (home/.chezmoidata/aliases.yaml): hands the whole
# command line to procs's completer.
name: pst
description: Alias for procs
parsing: disabled
completion:
  positionalany: ["$carapace.bridge.CarapaceBin([procs])"]
