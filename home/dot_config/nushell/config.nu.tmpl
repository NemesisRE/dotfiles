# chezmoi-managed thin nu entry-point stub (deployed on every OS, not just
# Linux) — see env.nu.tmpl's comment for why, and for why the real logic
# lives elsewhere.

source "{{ .chezmoi.homeDir }}/.local/share/nredf/shell/nushell/config.nu"
