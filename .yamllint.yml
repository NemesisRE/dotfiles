extends: default

rules:
  line-length:
    max: 160
  document-start:
    present: false
  truthy:
    allowed-values: ['true', 'false']
    check-keys: false
  comments:
    min-spaces-from-content: 1
