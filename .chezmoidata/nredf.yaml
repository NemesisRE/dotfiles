# nredf data for chezmoi (source of truth)
# Edit and run `chezmoi apply` to render the generated files.

generell:
  multiplexer: true
  ssh-agent: default # options: default, pipe, gpg, bitwarden

other:
  ssh_totp_provider: "#bitwarden"
